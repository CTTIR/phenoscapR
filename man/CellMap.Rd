% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{CellMap}
\alias{CellMap}
\title{Plot Cell Map}
\usage{
CellMap(
  object,
  colour_by = "phenotype",
  colours = NULL,
  pt_size = 0.5,
  title = NULL,
  dark_theme = FALSE
)
}
\arguments{
\item{object}{An \code{\link{AkoyaExperiment}} object.}

\item{colour_by}{Character. Column name in \code{meta_data} to colour
by. Default \code{"phenotype"}.}

\item{colours}{Named character vector or \code{NULL} for automatic
colours via the global palette.}

\item{pt_size}{Numeric. Point size. Default \code{0.5}.}

\item{title}{Character or \code{NULL}. Plot title.}

\item{dark_theme}{Logical. Use a dark background (useful for tissue
images). Default \code{FALSE}.}
}
\value{
A \code{ggplot} object.
}
\description{
Scatter plot of cell positions coloured by phenotype, cluster, or any
metadata column. Analogous to Seurat's \code{DimPlot} but in tissue
space.
}
<<<<<<< Updated upstream
\usage{
CellMap(object, colour_by = "phenotype", colours = NULL,
        pt_size = 0.5, title = NULL, dark_theme = FALSE)
}
\arguments{
  \item{object}{An \code{\link[=AkoyaExperiment-class]{AkoyaExperiment}} object.}
  \item{colour_by}{Character. Column name in \code{meta_data} to colour
    by. Default \code{"phenotype"}.}
  \item{colours}{Named character vector or \code{NULL} for automatic
    colours via the global palette.}
  \item{pt_size}{Numeric. Point size. Default \code{0.5}.}
  \item{title}{Character or \code{NULL}. Plot title.}
  \item{dark_theme}{Logical. Use a dark background (useful for tissue
    images). Default \code{FALSE}.}
}
\value{
A \code{ggplot} object.
}
=======
>>>>>>> Stashed changes
\examples{
counts <- matrix(rnorm(100), nrow = 50,
                 dimnames = list(NULL, c("CD3", "CD8")))
coords <- data.frame(x = runif(50, 0, 500), y = runif(50, 0, 500))
obj <- CreateAkoyaObject(counts, coords)
obj <- PhenotypeCells(obj, thresholds = list(CD3 = 0, CD8 = 0))
CellMap(obj)

}
