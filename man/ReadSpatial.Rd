% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{ReadSpatial}
\alias{ReadSpatial}
\title{Read Cell Segmentation Data into a SpatialCellData Object}
\usage{
ReadSpatial(
  path,
  sample_id = NULL,
  markers = NULL,
  filter = "DAPI|Blank|Empty",
  compartment = "Cell",
  statistic = "mean",
  sep = "auto",
  project = "SpatialProject"
)
}
\arguments{
\item{path}{Character. Path to a CSV file or a directory containing CSV
files.}

\item{sample_id}{Character or \code{NULL}. An identifier for the sample.}

\item{markers}{Character vector or \code{NULL}. If provided, only these
marker columns are retained.}

\item{filter}{Character or \code{NA}. Regex pattern for marker names to
exclude. Default \code{"DAPI|Blank|Empty"}.}

\item{compartment}{Character. Compartment for intensity extraction
(QuPath full format). Default \code{"Cell"}.}

\item{statistic}{Character. Summary statistic to extract
(QuPath full format). Default \code{"mean"}.}

\item{sep}{Character or \code{"auto"}. Column delimiter. Default
\code{"auto"}.}

\item{project}{Character. Project name. Default \code{"SpatialProject"}.}
}
\value{
A \code{\link{SpatialCellData-class}} object.
}
\description{
Reads cell segmentation CSV files and returns a
\code{\link{SpatialCellData-class}} object. This is the recommended
high-level entry point.
}
\examples{
tmp <- tempfile(fileext = ".csv")
write.csv(data.frame(
  `Cell ID` = 1:5,
  `Cell X Position` = runif(5, 0, 1000),
  `Cell Y Position` = runif(5, 0, 1000),
  CD3 = rnorm(5, 300, 80),
  CD8 = rnorm(5, 200, 60),
  check.names = FALSE
), tmp, row.names = FALSE)
obj <- ReadSpatial(tmp, filter = NA)
obj
unlink(tmp)

}
