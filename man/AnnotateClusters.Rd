% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{AnnotateClusters}
\alias{AnnotateClusters}
\title{Annotate Clusters by Their Top Markers}
\usage{
AnnotateClusters(
  object,
  group = "expr_cluster",
  slot = "data",
  n_markers = 2L,
  add_column = TRUE
)
}
\arguments{
\item{object}{A \code{\link{SpatialCellData-class}} object.}

\item{group}{Character. Metadata column holding cluster labels. Default
\code{"expr_cluster"}.}

\item{slot}{Character. \code{"data"} (default) or \code{"counts"}.}

\item{n_markers}{Integer. Number of top markers per cluster name. Default
\code{2}.}

\item{add_column}{Logical. If \code{TRUE} (default) also store the names in a
new \code{<group>_label} metadata column.}
}
\value{
A named character vector mapping each group level to its marker label.
If \code{add_column} the object is returned instead, with the label column
added (access the mapping via \code{attr(., "labels")}).
}
\description{
Labels each cluster (or any categorical grouping) by the markers most
enriched in it relative to the global mean, producing human-readable names
such as \code{"CD3+CD8"}.
}
\examples{
data(phenoscapR_example)
obj <- NormaliseData(phenoscapR_example, "zscore")
obj <- ExpressionClusters(obj, k = 6)
AnnotateClusters(obj, add_column = FALSE)

}
