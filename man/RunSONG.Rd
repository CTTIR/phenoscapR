% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reductions.R
\name{RunSONG}
\alias{RunSONG}
\title{SONG Embedding}
\usage{
RunSONG(
  object,
  dims = 30L,
  use_pca = TRUE,
  slot = "data",
  markers = NULL,
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{\link{SpatialCellData-class}} object.}

\item{dims}{Integer. Number of PCs (or markers) to embed. Default \code{30}.}

\item{use_pca}{Logical. Embed on PCA space (default \code{TRUE}) or directly
on the marker matrix.}

\item{slot}{Character. \code{"data"} (default) or \code{"counts"}.}

\item{markers}{Character vector or \code{NULL}.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility.}

\item{...}{Passed to \code{songR::song()}.}
}
\value{
The object with a \code{"song"} entry in its \code{reductions} slot.
}
\description{
Computes a 2-D SONG (Self-Organising Nebulous Growths) embedding via the
\pkg{songR} package and stores it under \code{"song"}. SONG is incremental,
noise-robust, and preserves global structure. By default the embedding is
computed on the top principal components (see \code{use_pca}).
}
\details{
\pkg{songR} is available from the CTTIR R-universe:
\code{install.packages("songR", repos = "https://cttir.r-universe.dev")}.
}
\examples{
\donttest{
if (requireNamespace("songR", quietly = TRUE)) {
  counts <- matrix(rnorm(500), nrow = 50,
                   dimnames = list(NULL, paste0("M", 1:10)))
  coords <- data.frame(x = runif(50), y = runif(50))
  obj <- CreateSpatialObject(counts, coords)
  obj <- RunSONG(obj, seed = 1)
}
}

}
