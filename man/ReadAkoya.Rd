% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{ReadAkoya}
\alias{ReadAkoya}
\title{Read Akoya Cell Segmentation Data (S4 interface)}
\usage{
ReadAkoya(
  path,
  type = c("auto", "processor", "inform", "qupath"),
  sample_id = NULL,
  markers = NULL,
  filter = "DAPI|Blank|Empty",
  recursive = TRUE,
  project = "AkoyaProject"
)
}
\arguments{
\item{path}{Character. Path to a CSV file or a directory containing CSV
files.}

\item{type}{Character. Input format: \code{"auto"} (default),
\code{"processor"}, \code{"inform"}, or \code{"qupath"}.}

\item{sample_id}{Character or \code{NULL}. An identifier for the sample.}

\item{markers}{Character vector or \code{NULL}. If provided, only these
marker columns are retained.}

\item{filter}{Character or \code{NA}. Regex pattern for marker names to
exclude. Default \code{"DAPI|Blank|Empty"}.}

\item{recursive}{Logical. Search subdirectories? Default \code{TRUE}.}

\item{project}{Character. Project name. Default \code{"AkoyaProject"}.}
}
\value{
An \code{\link{AkoyaExperiment-class}} object.
}
\description{
Reads cell segmentation CSV files produced by Akoya Biosciences platforms
(PhenoCycler, CODEX, PhenoImager) and returns an
\code{\link{AkoyaExperiment-class}} object.
}
\examples{
tmp <- tempfile(fileext = ".csv")
write.csv(data.frame(
  `Cell ID` = 1:5,
  `Cell X Position` = runif(5, 0, 1000),
  `Cell Y Position` = runif(5, 0, 1000),
  CD3 = rnorm(5, 300, 80),
  CD8 = rnorm(5, 200, 60),
  check.names = FALSE
), tmp, row.names = FALSE)
obj <- ReadAkoya(tmp, filter = NA)
obj
unlink(tmp)

}
