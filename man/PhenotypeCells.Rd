% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process.R
\name{PhenotypeCells}
\alias{PhenotypeCells}
\title{Phenotype Cells (SpatialCellData)}
\usage{
PhenotypeCells(object, thresholds, labels = NULL)
}
\arguments{
\item{object}{An \code{\link{SpatialCellData-class}} object.}

\item{thresholds}{Named list of thresholds (marker name = value).}

\item{labels}{Named character vector mapping signatures to labels, or
\code{NULL} for automatic labelling.}
}
\value{
An \code{\link{SpatialCellData-class}} with a \code{phenotype}
column in \code{meta_data}.
}
\description{
Assigns phenotype labels to cells based on marker intensity thresholds.
Uses the normalised \code{data} slot.
}
\examples{
counts <- matrix(c(rnorm(10, 5), rnorm(10, 1)), ncol = 2,
                 dimnames = list(NULL, c("CD3", "CD8")))
coords <- data.frame(x = runif(10), y = runif(10))
obj <- CreateSpatialObject(counts, coords)
obj <- PhenotypeCells(obj, thresholds = list(CD3 = 3, CD8 = 3))
table(Meta(obj)$phenotype)

}
