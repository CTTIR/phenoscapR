% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{FindNeighbours}
\alias{FindNeighbours}
\title{Find Nearest Neighbours (SpatialCellData)}
\usage{
FindNeighbours(object, k = 1L, target = NULL)
}
\arguments{
\item{object}{An \code{\link{SpatialCellData-class}} object.}

\item{k}{Integer. Number of nearest neighbours. Default \code{1}.}

\item{target}{Character or \code{NULL}. Restrict to a specific phenotype.}
}
\value{
An \code{\link{SpatialCellData-class}} with \code{nn_distance}
added to \code{meta_data} and stored in the \code{spatial} slot.
}
\description{
For each cell, computes the distance to the \code{k} nearest neighbours.
}
\examples{
counts <- matrix(rnorm(40), nrow = 20,
                 dimnames = list(NULL, c("CD3", "CD8")))
coords <- data.frame(x = runif(20, 0, 100), y = runif(20, 0, 100))
obj <- CreateSpatialObject(counts, coords)
obj <- FindNeighbours(obj, k = 3)

}
