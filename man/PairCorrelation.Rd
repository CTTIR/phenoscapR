% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{PairCorrelation}
\alias{PairCorrelation}
\title{Pair Correlation Function (g(r))}
\description{
Computes the pair correlation function, the derivative of Ripley's K,
which measures spatial clustering/inhibition at specific distances.
Values above 1 indicate clustering, below 1 indicate inhibition.
}
\usage{
PairCorrelation(object, r_seq = NULL, dr = NULL,
                target = NULL)
}
\arguments{
  \item{object}{An \code{\link{AkoyaExperiment}} object.}
  \item{r_seq}{Numeric vector. Distances. If \code{NULL}, auto-computed.}
  \item{dr}{Numeric. Annulus width. Default is range / 40.}
  \item{target}{Character or \code{NULL}. Restrict to this phenotype.}
}
\value{
A data frame with columns \code{r} and \code{g}.
}
\examples{
set.seed(1)
counts <- matrix(rnorm(200), nrow = 100,
                 dimnames = list(NULL, c("CD3", "CD8")))
coords <- data.frame(x = runif(100, 0, 500), y = runif(100, 0, 500))
obj <- CreateAkoyaObject(counts, coords)
pcf <- PairCorrelation(obj)
head(pcf)
}
