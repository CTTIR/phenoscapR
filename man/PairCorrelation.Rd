% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{PairCorrelation}
\alias{PairCorrelation}
\title{Pair Correlation Function}
\usage{
PairCorrelation(
  object,
  r_seq = NULL,
  dr = NULL,
  target = NULL,
  by_sample = FALSE
)
}
\arguments{
\item{object}{An \code{\link{SpatialCellData-class}} object.}

\item{r_seq}{Numeric vector of radii, or \code{NULL} for automatic.}

\item{dr}{Numeric or \code{NULL}. Ring width. Default is derived from
\code{r_seq}.}

\item{target}{Character or \code{NULL}. Restrict to a phenotype.}

\item{by_sample}{Logical. If \code{TRUE} and the object holds several samples,
the statistic is computed per sample and returned as a named list. Default
\code{FALSE}; otherwise a single sample is required.}
}
\value{
A data frame with columns \code{r} and \code{g}.
}
\description{
Computes the pair correlation function g(r), the derivative of
Ripley's K, measuring clustering/inhibition at specific distances.
}
\examples{
counts <- matrix(rnorm(100), nrow = 50,
                 dimnames = list(NULL, c("CD3", "CD8")))
coords <- data.frame(x = runif(50, 0, 200), y = runif(50, 0, 200))
obj <- CreateSpatialObject(counts, coords)
PairCorrelation(obj)

}
