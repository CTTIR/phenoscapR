% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualise.R
\name{plot_heatmap}
\alias{plot_heatmap}
\title{Plot Marker Heatmap}
\usage{
plot_heatmap(dt, markers = NULL, title = NULL)
}
\arguments{
\item{dt}{A \code{data.table} with marker columns and a \code{phenotype} column.}

\item{markers}{Character vector or \code{NULL}. Markers to include. If \code{NULL},
all marker columns are used.}

\item{title}{Character string or \code{NULL}. Plot title.}
}
\value{
A \code{ggplot} object.
}
\description{
Displays a heatmap of mean marker intensities per phenotype.
}
\examples{
dt <- data.table::data.table(
  sample_id = "s1", cell_id = 1:60,
  x = runif(60), y = runif(60),
  CD3 = c(rnorm(30, 1), rnorm(30, 0)),
  CD8 = c(rnorm(30, 0), rnorm(30, 1)),
  phenotype = rep(c("CD3+", "CD8+"), each = 30)
)
plot_heatmap(dt)
}
