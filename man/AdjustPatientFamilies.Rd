% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference-exact.R
\name{AdjustPatientFamilies}
\alias{AdjustPatientFamilies}
\title{Adjust Explicitly Declared Patient-Test Families}
\usage{
AdjustPatientFamilies(tests, policy, method = c("BH", "BY"))
}
\arguments{
\item{tests}{Data frame containing character \code{hypothesis_id}, \code{family_id},
and numeric \code{p_value}. All declared hypotheses must be present exactly once.}

\item{policy}{Explicit adjustment policy, \code{"declared"} or \code{"finite_only"}.}

\item{method}{Selected correction, \code{"BH"} or \code{"BY"}.}
}
\value{
A list with \code{tests} (input columns plus all adjustment variants,
selected \code{p_adjusted}, policy and method) and \code{families} (declared, testable
and unavailable counts). Every NA is retained in the original row order.
}
\description{
Computes finite-only BH for historical comparison and declared-family BH/BY
using every registered hypothesis as the adjustment count. Unavailable p
values remain NA. Family IDs define the entire correction scope: include
setting and contrast in the ID only when separate families are intended.
A retrospective family registry is not prospective registration, and neither
adjustment establishes biological validity or study-wide error control.
}
\examples{
x <- data.frame(hypothesis_id = c("h1", "h2"), family_id = "f",
                p_value = c(0.03, NA))
AdjustPatientFamilies(x, policy = "declared")
}
