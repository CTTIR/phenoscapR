% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process.R
\name{normalise_markers}
\alias{normalise_markers}
\title{Normalise Marker Intensities}
\usage{
normalise_markers(
  dt,
  method = c("zscore", "minmax", "quantile"),
  markers = NULL
)
}
\arguments{
\item{dt}{A \code{data.table} as returned by \code{\link[=read_akoya]{read_akoya()}}.}

\item{method}{Character string. Normalisation method: \code{"zscore"} (default),
\code{"minmax"}, or \code{"quantile"}.}

\item{markers}{Character vector or \code{NULL}. Marker columns to normalise.
If \code{NULL}, all detected marker columns are normalised.}
}
\value{
A \code{data.table} with normalised marker intensities.
}
\description{
Normalises marker intensity columns using one of several methods.
}
\examples{
dt <- data.table::data.table(
  sample_id = "s1", cell_id = 1:20,
  x = runif(20), y = runif(20),
  CD3 = rnorm(20, 500, 100),
  CD8 = rnorm(20, 300, 80)
)
norm_dt <- normalise_markers(dt, method = "zscore")
head(norm_dt)

}
