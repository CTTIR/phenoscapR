% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{nearest_neighbours}
\alias{nearest_neighbours}
\title{Compute Nearest Neighbour Distances}
\usage{
nearest_neighbours(dt, target_phenotype = NULL, k = 1L)
}
\arguments{
\item{dt}{A \code{data.table} with columns \code{x} and \code{y}.}

\item{target_phenotype}{Character string or \code{NULL}. If provided, distances
are computed only to cells of this phenotype. Requires a \code{phenotype}
column.}

\item{k}{Integer. Number of nearest neighbours to consider. Default \code{1}.}
}
\value{
The input \code{data.table} with an added \code{nn_distance} column
(mean distance to the \code{k} nearest neighbours).
}
\description{
For each cell, computes the distance to the nearest neighbouring cell,
optionally restricted to specific phenotypes.
}
\examples{
dt <- data.table::data.table(
  sample_id = "s1", cell_id = 1:20,
  x = runif(20, 0, 100), y = runif(20, 0, 100)
)
result <- nearest_neighbours(dt, k = 3)
head(result[, .(cell_id, nn_distance)])

}
