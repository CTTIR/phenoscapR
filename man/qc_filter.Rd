% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process.R
\name{qc_filter}
\alias{qc_filter}
\title{Quality Control Filter}
\usage{
qc_filter(
  dt,
  min_area = NULL,
  max_area = NULL,
  min_intensity = 0,
  max_intensity = NULL
)
}
\arguments{
\item{dt}{A \code{data.table} as returned by \code{\link[=read_spatial]{read_spatial()}}.}

\item{min_area}{Numeric or \code{NULL}. Minimum cell area. Cells below this
threshold are removed.}

\item{max_area}{Numeric or \code{NULL}. Maximum cell area. Cells above this
threshold are removed.}

\item{min_intensity}{Numeric. Minimum total marker intensity. Cells with
total intensity below this value are removed. Default \code{0}.}

\item{max_intensity}{Numeric or \code{NULL}. Maximum total marker intensity.}
}
\value{
A filtered \code{data.table}.
}
\description{
Removes cells that fail quality control criteria based on marker intensity
ranges, cell area, or custom filters.
}
\examples{
dt <- data.table::data.table(
  sample_id = "s1", cell_id = 1:10,
  x = runif(10), y = runif(10),
  cell_area = c(5, seq(50, 200, length.out = 8), 5000),
  CD3 = rnorm(10, 300, 50)
)
filtered <- qc_filter(dt, min_area = 10, max_area = 1000)
nrow(filtered)

}
