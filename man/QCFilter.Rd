% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process.R
\name{QCFilter}
\alias{QCFilter}
\title{Quality Control Filter (SpatialCellData)}
\usage{
QCFilter(
  object,
  min_area = NULL,
  max_area = NULL,
  min_intensity = 0,
  max_intensity = NULL
)
}
\arguments{
\item{object}{An \code{\link{SpatialCellData-class}} object.}

\item{min_area}{Numeric or \code{NULL}. Minimum cell area.}

\item{max_area}{Numeric or \code{NULL}. Maximum cell area.}

\item{min_intensity}{Numeric. Minimum total marker intensity. Default \code{0}.}

\item{max_intensity}{Numeric or \code{NULL}. Maximum total marker intensity.}
}
\value{
A filtered \code{\link{SpatialCellData-class}} object.
}
\description{
Removes cells that fail quality control criteria based on cell area
and/or total marker intensity.
}
\examples{
counts <- matrix(rnorm(60, 5), nrow = 20,
                 dimnames = list(NULL, c("CD3", "CD8", "CD20")))
coords <- data.frame(x = runif(20), y = runif(20))
meta <- data.frame(cell_id = 1:20, sample_id = "s1",
                   cell_area = c(5, seq(50, 200, length.out = 18), 5000))
obj <- CreateSpatialObject(counts, coords, meta)
filtered <- QCFilter(obj, min_area = 10, max_area = 1000)
NCells(filtered)

}
