% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{NeighbourhoodEnrichment}
\alias{NeighbourhoodEnrichment}
\title{Neighbourhood Enrichment Analysis}
\usage{
NeighbourhoodEnrichment(object, radius, n_perm = 100L, seed = NULL)
}
\arguments{
\item{object}{An \code{\link{SpatialCellData-class}} object.}

\item{radius}{Numeric. Neighbourhood radius.}

\item{n_perm}{Integer. Number of permutations. Default \code{100}.}

\item{seed}{Integer or \code{NULL}. Random seed.}
}
\value{
A data frame with columns \code{from}, \code{to}, \code{observed},
\code{mean_expected}, \code{z_score}, and \code{p_value}.
}
\description{
Tests whether specific phenotype pairs are spatially enriched or depleted
relative to a random permutation baseline.
}
\examples{
set.seed(1)
counts <- matrix(rnorm(200), nrow = 100,
                 dimnames = list(NULL, c("CD3", "CD8")))
coords <- data.frame(x = runif(100, 0, 500), y = runif(100, 0, 500))
meta <- data.frame(cell_id = 1:100, sample_id = "s1",
  phenotype = sample(c("A", "B"), 100, replace = TRUE))
obj <- CreateSpatialObject(counts, coords, meta)
NeighbourhoodEnrichment(obj, radius = 50, n_perm = 10)

}
