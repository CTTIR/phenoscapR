% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process.R
\name{NormaliseData}
\alias{NormaliseData}
\title{Normalise Marker Intensities (AkoyaExperiment)}
\usage{
NormaliseData(
  object,
  method = c("zscore", "minmax", "quantile"),
  markers = NULL
)
}
\arguments{
\item{object}{An \code{\link{AkoyaExperiment-class}} object.}

\item{method}{Character. \code{"zscore"} (default), \code{"minmax"}, or
\code{"quantile"}.}

\item{markers}{Character vector or \code{NULL}. Markers to normalise.
If \code{NULL}, all markers are normalised.}
}
\value{
An \code{\link{AkoyaExperiment-class}} with updated \code{data} slot.
}
\description{
Normalises marker intensities and stores the result in the \code{data} slot.
Raw counts remain unchanged.
}
\examples{
counts <- matrix(rnorm(40, 500, 100), nrow = 20,
                 dimnames = list(NULL, c("CD3", "CD8")))
coords <- data.frame(x = runif(20), y = runif(20))
obj <- CreateAkoyaObject(counts, coords)
obj <- NormaliseData(obj, method = "zscore")

}
