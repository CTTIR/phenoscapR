% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{CreateSpatialObject}
\alias{CreateSpatialObject}
\title{Create a SpatialCellData Object from Existing Data}
\usage{
CreateSpatialObject(
  counts,
  coords,
  meta_data = NULL,
  sample_id = "sample1",
  project = "SpatialProject"
)
}
\arguments{
\item{counts}{Numeric matrix (cells x markers). Row names are optional.}

\item{coords}{Data frame with columns \code{x} and \code{y}.}

\item{meta_data}{Data frame of per-cell metadata, or \code{NULL}.}

\item{sample_id}{Character. Sample identifier. Default \code{"sample1"}.}

\item{project}{Character. Project name. Default \code{"SpatialProject"}.}
}
\value{
A \code{\link{SpatialCellData-class}} object.
}
\description{
Constructs a \code{\link{SpatialCellData-class}} object from a counts matrix,
coordinate data frame, and optional metadata.
}
\examples{
counts <- matrix(rnorm(50), nrow = 10,
                 dimnames = list(NULL, c("CD3", "CD8", "CD20", "DAPI", "PanCK")))
coords <- data.frame(x = runif(10, 0, 500), y = runif(10, 0, 500))
obj <- CreateSpatialObject(counts, coords, sample_id = "mysample")
obj

}
