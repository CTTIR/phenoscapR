% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualise.R
\name{plot_cell_map}
\alias{plot_cell_map}
\title{Plot Cell Map}
\usage{
plot_cell_map(
  dt,
  colour_by = "phenotype",
  colours = NULL,
  point_size = 0.5,
  title = NULL
)
}
\arguments{
\item{dt}{A \code{data.table} with columns \code{x} and \code{y}.}

\item{colour_by}{Character string. Column name to colour cells by.
Default \code{"phenotype"}.}

\item{colours}{Named character vector of colours, or \code{NULL} for automatic
colours.}

\item{point_size}{Numeric. Size of plotted points. Default \code{0.5}.}

\item{title}{Character string or \code{NULL}. Plot title.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a scatter plot of cell positions, coloured by phenotype or cluster.
}
\examples{
dt <- data.table::data.table(
  sample_id = "s1", cell_id = 1:50,
  x = runif(50, 0, 500), y = runif(50, 0, 500),
  phenotype = sample(c("CD3+", "CD8+", "Negative"), 50, replace = TRUE)
)
plot_cell_map(dt)

}
