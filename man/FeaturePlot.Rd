% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{FeaturePlot}
\alias{FeaturePlot}
\title{Feature Plot in Tissue Space}
\description{
Plots marker expression intensity on spatial coordinates. Each marker
gets its own panel. Similar to Seurat's \code{FeaturePlot} but in
tissue space.
}
\usage{
FeaturePlot(object, features, slot = "data", pt_size = 0.3,
            ncol = NULL, palette = NULL, dark_theme = FALSE)
}
\arguments{
  \item{object}{An \code{\link[=AkoyaExperiment-class]{AkoyaExperiment}} object.}
  \item{features}{Character vector. Marker names to plot.}
  \item{slot}{Character. \code{"data"} or \code{"counts"}.}
  \item{pt_size}{Numeric. Point size. Default \code{0.3}.}
  \item{ncol}{Integer. Number of columns in the faceted layout. Default
    \code{NULL} (auto).}
  \item{palette}{Character or character vector. Colour palette. Default
    \code{NULL} uses the global palette.}
  \item{dark_theme}{Logical. Dark background. Default \code{FALSE}.}
}
\value{
A \code{ggplot} object.
}
\examples{
counts <- matrix(rnorm(150), nrow = 50,
                 dimnames = list(NULL, c("CD3", "CD8", "PanCK")))
coords <- data.frame(x = runif(50, 0, 500), y = runif(50, 0, 500))
obj <- CreateAkoyaObject(counts, coords)
FeaturePlot(obj, features = c("CD3", "CD8"))
}
