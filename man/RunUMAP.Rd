% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reductions.R
\name{RunUMAP}
\alias{RunUMAP}
\title{UMAP Embedding}
\usage{
RunUMAP(
  object,
  dims = 30L,
  use_pca = TRUE,
  n_neighbors = 15L,
  densmap = FALSE,
  slot = "data",
  markers = NULL,
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{\link{SpatialCellData-class}} object.}

\item{dims}{Integer. Number of PCs (or markers) to embed. Default \code{30}.}

\item{use_pca}{Logical. Embed on PCA space (default \code{TRUE}) or directly
on the marker matrix.}

\item{n_neighbors}{Integer. UMAP neighbourhood size. Default \code{15}.}

\item{densmap}{Logical. Use density-preserving densMAP instead of UMAP.
Default \code{FALSE}. Requires a recent \pkg{uwot}.}

\item{slot}{Character. \code{"data"} (default) or \code{"counts"}.}

\item{markers}{Character vector or \code{NULL}.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility.}

\item{...}{Passed to \code{uwot::umap()} / \code{uwot::densmap()}.}
}
\value{
The object with a \code{"umap"} entry in its \code{reductions} slot.
}
\description{
Computes a 2-D UMAP embedding via the \pkg{uwot} package and stores it under
\code{"umap"}. By default the embedding is computed on the top principal
components (see \code{use_pca}).
}
\examples{
\donttest{
if (requireNamespace("uwot", quietly = TRUE)) {
  counts <- matrix(rnorm(500), nrow = 50,
                   dimnames = list(NULL, paste0("M", 1:10)))
  coords <- data.frame(x = runif(50), y = runif(50))
  obj <- CreateSpatialObject(counts, coords)
  obj <- RunUMAP(obj, seed = 1)
}
}

}
